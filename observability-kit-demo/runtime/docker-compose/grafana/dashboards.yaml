apiVersion: 1

providers:
  - name: Vaadin    # A uniquely identifiable name for the provider
    folder: Vaadin  # The folder where to place the dashboards
    type: file
    options:
      path:  /var/lib/grafana/dashboards
