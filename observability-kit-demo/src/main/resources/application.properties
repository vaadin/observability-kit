# Default port; override locally with the PORT environment variable when 8080
# is taken, e.g. PORT=8082 mvn spring-boot:run -pl :observability-kit-demo
server.port=${PORT:8080}
management.endpoints.web.exposure.include=vaadin,prometheus,metrics
management.endpoint.vaadin.access=unrestricted
management.endpoint.prometheus.access=unrestricted
